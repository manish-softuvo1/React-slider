import React from 'react';
import ReactDOM from 'react-dom';
import ImageList from './components/slider';

ReactDOM.render(
  <React.StrictMode>
    <ImageList />
  </React.StrictMode>,
  document.getElementById('root')
);

